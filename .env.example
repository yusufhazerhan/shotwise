# Shotwise local Studio does not require env vars.
# Keep this file intentionally tiny for the open-source app.
# Copy to .env.local only if you need to override local URLs.
APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000
